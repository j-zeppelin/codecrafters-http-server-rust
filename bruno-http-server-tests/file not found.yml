info:
  name: file not found
  type: http
  seq: 6

http:
  method: GET
  url: http://127.0.0.1:4221/files/abasdfa.txt
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
