info:
  name: root
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:4221
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
